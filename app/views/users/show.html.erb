<%= render "shared/header"%>

<h2>ユーザーページ</h2>

<%= link_to "編集", edit_user_path(@user) %>

<% @user_books.map do |user_book| %>
  <%= link_to image_tag(user_book.book_image), "#" if user_book.book_image.attached? %>
<% end %>